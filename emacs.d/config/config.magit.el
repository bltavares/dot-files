;;; config.magit.el --- Package
;;; Commentary:
;;; Code:

(require 'magit)
(setq magit-use-overlays nil)
(setq magit-last-seen-setup-instructions "1.4.0")

(provide 'config.magit)
;;; config.magit ends here
